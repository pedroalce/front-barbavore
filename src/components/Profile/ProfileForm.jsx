export default function ProfileForm() {
  // Em breve: renderizar formulário de perfil do cliente
  return (
    <div>
      <h3>Perfil do Cliente (em breve)</h3>
      {/* Renderizar campos de perfil, upload de foto, etc. */}
    </div>
  );
}
